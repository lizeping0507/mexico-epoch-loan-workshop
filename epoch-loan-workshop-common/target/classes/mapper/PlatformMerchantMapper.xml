<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epoch.loan.workshop.common.dao.PlatformMerchantDao">

    <select id="findMerchant" resultType="com.epoch.loan.workshop.common.entity.PlatformMerchantEntity">
        SELECT * FROM platform_merchant WHERE id=#{merchantId} LIMIT 0,1
    </select>
</mapper>