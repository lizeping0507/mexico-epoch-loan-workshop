<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epoch.loan.workshop.common.dao.mysql.LoanProductExtDao">

    <select id="findByProductId" resultType="com.epoch.loan.workshop.common.entity.mysql.LoanProductExtEntity">
        SELECT * FROM loan_product_ext WHERE product_id=#{productId} LIMIT 0,1
    </select>
</mapper>