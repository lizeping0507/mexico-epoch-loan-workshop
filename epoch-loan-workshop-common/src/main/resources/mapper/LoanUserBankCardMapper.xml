<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epoch.loan.workshop.common.dao.mysql.LoanUserBankCardDao">
 <select id="findByUserId" resultType="com.epoch.loan.workshop.common.entity.mysql.LoanUserBankCardEntity">
     SELECT user_id,card_id,bank_card,open_bank FROM loan_user_bank_card WHERE user_id = #{userId}
 </select>
</mapper>