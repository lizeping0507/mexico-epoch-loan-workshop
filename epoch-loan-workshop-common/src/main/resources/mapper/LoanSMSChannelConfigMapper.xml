<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epoch.loan.workshop.common.dao.mysql.LoanSMSChannelConfigDao">

    <select id="findSMSChannelConfigListByStatus"
            resultType="com.epoch.loan.workshop.common.entity.mysql.LoanSMSChannelConfigEntity">
        SELECT * FROM loan_sms_channel_config WHERE status=#{status}
    </select>
</mapper>